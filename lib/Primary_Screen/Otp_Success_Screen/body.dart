import 'package:flutter/material.dart';

import '../Otp_Success_Screen/Screen/otp_Screen.dart';

class OtpSuccess extends StatelessWidget {
  const OtpSuccess({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body:  SingleChildScrollView(child: Body()));
  }
}
